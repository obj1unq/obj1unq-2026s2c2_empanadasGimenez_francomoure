import empleados.*
/*
test "punto1: Planteo inicial" {

  baigorria.vender(500)
  galvan.sueldo(20000)
  baigorria.vender(300)
  assert.equals(12000, baigorria.sueldo())
  assert.equals(20000, galvan.sueldo())
  
  gimenez.pagarSueldo(baigorria)
  gimenez.pagarSueldo(galvan)
  assert.equals(268000, gimenez.fondo())
  //si gimanez no tiene dinero para pagar un sueldo, debe lanzar error
}

/*
agreagar dos test con respecto a esto

method pagarSueldo(empleado){
        fondo -= empleado.getSueldo()
        empleado.cobrarSueldo()
    }


 test "pepita vuela lo que se le antoja" {
   pepita.dado(dadoCargado) // Le configuro el stub
   pepita.volarLoQueSeTeAntoje() //Se que voló 3
   assert.equals(87, pepita.energia()) //verifico el efecto sabiendo 
                                     // que se le antojó 3
 }

*/

describe "hacer que pague sueldos"{
test "hacer que pague sueldos"{

    /*
    debo hacer arrange,act y assert
    */

    gimenez.pagarSueldo(baigorria)
    gimenez.pagarSueldo(galvan)
    assert.equals(300000, gimenez.fondo())
}
}

